---
# Test playbook for win_ms_network_server_idle_timeout role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-ms-network-server-idle-timeout-dev.yml

- name: Test Windows Microsoft Network Server Idle Timeout Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_ms_network_server_idle_timeout  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for AutoDisconnect configuration."
