---
# Test playbook for win_windows_push_notifications_system_service_disabled role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-push-notifications-system-service-disabled-dev.yml

- name: Test Windows Push Notifications System Service Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_windows_push_notifications_system_service_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
