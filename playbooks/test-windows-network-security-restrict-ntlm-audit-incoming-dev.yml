---
# Test playbook for win_network_security_restrict_ntlm_audit_incoming role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-network-security-restrict-ntlm-audit-incoming-dev.yml

- name: Test Windows Network Security Restrict NTLM Audit Incoming Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_network_security_restrict_ntlm_audit_incoming  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for AuditReceivingNTLMTraffic configuration."
