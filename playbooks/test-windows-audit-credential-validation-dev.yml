---
# Test playbook for win_audit_credential_validation role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-audit-credential-validation-dev.yml

- name: Test Windows Audit Credential Validation Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_credential_validation  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for Credential Validation audit policy configuration."
