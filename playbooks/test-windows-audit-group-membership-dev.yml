---
# Test playbook for win_audit_group_membership role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-audit-group-membership-dev.yml

- name: Test Windows Audit Group Membership Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_group_membership  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
