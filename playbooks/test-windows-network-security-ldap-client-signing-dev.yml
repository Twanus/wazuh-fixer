---
# Test playbook for win_network_security_ldap_client_signing role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-network-security-ldap-client-signing-dev.yml

- name: Test Windows Network Security LDAP Client Signing Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_network_security_ldap_client_signing  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
