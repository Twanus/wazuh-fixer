---
# Test playbook for win_audit_other_policy_change_events role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-audit-other-policy-change-events-dev.yml

- name: Test Windows Audit Other Policy Change Events Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_other_policy_change_events  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
