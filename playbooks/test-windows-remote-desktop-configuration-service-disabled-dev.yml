---
# Test playbook for win_remote_desktop_configuration_service_disabled role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-remote-desktop-configuration-service-disabled-dev.yml

- name: Test Windows Remote Desktop Configuration Service Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_remote_desktop_configuration_service_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
