---
# Test playbook for win_audit_system_integrity role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-audit-system-integrity-dev.yml

- name: Test Windows Audit System Integrity Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_system_integrity  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for System Integrity audit policy configuration."
