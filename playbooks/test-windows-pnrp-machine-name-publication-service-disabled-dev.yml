---
# Test playbook for win_pnrp_machine_name_publication_service_disabled role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-pnrp-machine-name-publication-service-disabled-dev.yml

- name: Test Windows PNRP Machine Name Publication Service Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_pnrp_machine_name_publication_service_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
