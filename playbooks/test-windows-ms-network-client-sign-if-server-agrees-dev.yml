---
# Test playbook for win_ms_network_client_sign_if_server_agrees role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-ms-network-client-sign-if-server-agrees-dev.yml

- name: Test Windows Microsoft Network Client Sign If Server Agrees Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_ms_network_client_sign_if_server_agrees  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for EnableSecuritySignature configuration."
