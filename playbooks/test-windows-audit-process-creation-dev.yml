---
# Test playbook for win_audit_process_creation role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-audit-process-creation-dev.yml

- name: Test Windows Audit Process Creation Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_process_creation  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
