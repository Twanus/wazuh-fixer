---
# Test playbook for win_system_objects_strengthen_default_permissions role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-system-objects-strengthen-default-permissions-dev.yml

- name: Test Windows System Objects Strengthen Default Permissions Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_system_objects_strengthen_default_permissions  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for ProtectionMode configuration."
