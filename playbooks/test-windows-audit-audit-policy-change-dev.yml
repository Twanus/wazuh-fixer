---
# Test playbook for win_audit_audit_policy_change role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-audit-audit-policy-change-dev.yml

- name: Test Windows Audit Audit Policy Change Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_audit_policy_change  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
