---
# Test playbook for win_online_speech_recognition_disabled role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-online-speech-recognition-disabled-dev.yml

- name: Test Windows Online Speech Recognition Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_online_speech_recognition_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for AllowInputPersonalization configuration."
