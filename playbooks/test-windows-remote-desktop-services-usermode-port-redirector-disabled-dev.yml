---
# Test playbook for win_remote_desktop_services_usermode_port_redirector_disabled role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-remote-desktop-services-usermode-port-redirector-disabled-dev.yml

- name: Test Windows Remote Desktop Services UserMode Port Redirector Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_remote_desktop_services_usermode_port_redirector_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
