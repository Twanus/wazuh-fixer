---
# Test playbook for win_network_security_lan_manager_auth_level role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-network-security-lan-manager-auth-level-dev.yml

- name: Test Windows Network Security LAN Manager Auth Level Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_network_security_lan_manager_auth_level  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
