---
# Test playbook for win_windows_error_reporting_service_disabled role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-windows-error-reporting-service-disabled-dev.yml

- name: Test Windows Error Reporting Service Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_windows_error_reporting_service_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for Windows Error Reporting Service (WerSvc) Start configuration."
