---
# Test playbook for win_interactive_logon_require_ctrl_alt_del role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-interactive-logon-require-ctrl-alt-del-dev.yml

- name: Test Windows Interactive Logon Require CTRL+ALT+DEL Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_interactive_logon_require_ctrl_alt_del  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for DisableCAD configuration."
