---
# Test playbook for win_network_security_allow_localsystem_null_session_fallback role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-network-security-allow-localsystem-null-session-fallback-dev.yml

- name: Test Windows Network Security Allow LocalSystem NULL Session Fallback Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_network_security_allow_localsystem_null_session_fallback  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for AllowNullSessionFallback configuration."
