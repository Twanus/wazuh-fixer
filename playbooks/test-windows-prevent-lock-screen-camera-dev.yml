---
# Test playbook for win_prevent_lock_screen_camera role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-prevent-lock-screen-camera-dev.yml

- name: Test Windows Prevent Lock Screen Camera Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_prevent_lock_screen_camera  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
