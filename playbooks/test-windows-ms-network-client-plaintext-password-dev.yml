---
# Test playbook for win_ms_network_client_plaintext_password role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-ms-network-client-plaintext-password-dev.yml

- name: Test Windows Microsoft Network Client Plaintext Password Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_ms_network_client_plaintext_password  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for EnablePlainTextPassword configuration."
