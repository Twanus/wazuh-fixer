---
# Test playbook for win_interactive_logon_machine_inactivity_limit role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-interactive-logon-machine-inactivity-limit-dev.yml

- name: Test Windows Interactive Logon Machine Inactivity Limit Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_interactive_logon_machine_inactivity_limit  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for InactivityTimeoutSecs configuration."
