---
# Test playbook for win_audit_other_system_events role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-audit-other-system-events-dev.yml

- name: Test Windows Audit Other System Events Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_other_system_events  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
