---
# Test playbook for win_audit_removable_storage role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-audit-removable-storage-dev.yml

- name: Test Windows Audit Removable Storage Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_removable_storage  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for Removable Storage audit policy configuration."
