---
# Test playbook for win_interactive_logon_password_expiry_warning role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-interactive-logon-password-expiry-warning-dev.yml

- name: Test Windows Interactive Logon Password Expiry Warning Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_interactive_logon_password_expiry_warning  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for PasswordExpiryWarning configuration."
