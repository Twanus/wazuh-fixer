---
# Test playbook for win_remote_access_auto_connection_manager_disabled role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-remote-access-auto-connection-manager-disabled-dev.yml

- name: Test Windows Remote Access Auto Connection Manager Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_remote_access_auto_connection_manager_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for RasAuto Start configuration."
