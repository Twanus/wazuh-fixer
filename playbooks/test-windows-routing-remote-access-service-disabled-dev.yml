---
# Test playbook for win_routing_remote_access_service_disabled role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-routing-remote-access-service-disabled-dev.yml

- name: Test Windows Routing and Remote Access Service Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_routing_remote_access_service_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for RemoteAccess Start configuration."
