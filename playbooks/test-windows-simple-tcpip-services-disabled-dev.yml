---
# Test playbook for win_simple_tcpip_services_disabled role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-simple-tcpip-services-disabled-dev.yml

- name: Test Windows Simple TCP/IP Services Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_simple_tcpip_services_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
