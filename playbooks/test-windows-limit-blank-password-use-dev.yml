---
# Test playbook for win_limit_blank_password_use role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-limit-blank-password-use-dev.yml

- name: Test Windows Limit Blank Password Use Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_limit_blank_password_use  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for LimitBlankPasswordUse configuration."
