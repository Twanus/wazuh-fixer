---
# Test playbook for win_interactive_logon_dont_display_last_user role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-interactive-logon-dont-display-last-user-dev.yml

- name: Test Windows Interactive Logon Don't Display Last User Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_interactive_logon_dont_display_last_user  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for DontDisplayLastUserName configuration."
