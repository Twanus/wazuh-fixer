---
# Test playbook for win_link_layer_topology_discovery_mapper_disabled role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-link-layer-topology-discovery-mapper-disabled-dev.yml

- name: Test Windows Link-Layer Topology Discovery Mapper Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_link_layer_topology_discovery_mapper_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for lltdsvc Start configuration."
