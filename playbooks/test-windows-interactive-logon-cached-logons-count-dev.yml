---
# Test playbook for win_interactive_logon_cached_logons_count role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-interactive-logon-cached-logons-count-dev.yml

- name: Test Windows Interactive Logon Cached Logons Count Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_interactive_logon_cached_logons_count  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for CachedLogonsCount configuration."
