---
# Test playbook for win_ms_network_server_spn_validation role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-ms-network-server-spn-validation-dev.yml

- name: Test Windows Microsoft Network Server SPN Validation Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_ms_network_server_spn_validation  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for SMBServerNameHardeningLevel configuration."
