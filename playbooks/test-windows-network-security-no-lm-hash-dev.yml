---
# Test playbook for win_network_security_no_lm_hash role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-network-security-no-lm-hash-dev.yml

- name: Test Windows Network Security No LM Hash Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_network_security_no_lm_hash  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for NoLMHash configuration."
