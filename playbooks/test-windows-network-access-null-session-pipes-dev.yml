---
# Test playbook for win_network_access_null_session_pipes role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-network-access-null-session-pipes-dev.yml

- name: Test Windows Network Access Null Session Pipes Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_network_access_null_session_pipes  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for NullSessionPipes configuration."
