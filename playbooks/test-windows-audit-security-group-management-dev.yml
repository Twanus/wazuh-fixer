---
# Test playbook for win_audit_security_group_management role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-audit-security-group-management-dev.yml

- name: Test Windows Audit Security Group Management Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_security_group_management  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for Security Group Management audit policy configuration."
