---
# Test playbook for win_peer_networking_identity_manager_disabled role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-peer-networking-identity-manager-disabled-dev.yml

- name: Test Windows Peer Networking Identity Manager Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_peer_networking_identity_manager_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
