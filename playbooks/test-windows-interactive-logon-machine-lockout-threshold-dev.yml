---
# Test playbook for win_interactive_logon_machine_lockout_threshold role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-interactive-logon-machine-lockout-threshold-dev.yml

- name: Test Windows Interactive Logon Machine Lockout Threshold Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_interactive_logon_machine_lockout_threshold  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for MaxDevicePasswordFailedAttempts configuration."
