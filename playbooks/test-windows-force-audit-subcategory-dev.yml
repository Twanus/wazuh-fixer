---
# Test playbook for win_force_audit_subcategory role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-force-audit-subcategory-dev.yml

- name: Test Windows Force Audit Subcategory Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_force_audit_subcategory  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for SCENoApplyLegacyAuditPolicy configuration."
