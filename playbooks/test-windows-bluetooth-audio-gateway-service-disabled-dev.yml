---
# Test playbook for win_bluetooth_audio_gateway_service_disabled role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-bluetooth-audio-gateway-service-disabled-dev.yml

- name: Test Windows Bluetooth Audio Gateway Service Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_bluetooth_audio_gateway_service_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for BTAGService Start configuration."
