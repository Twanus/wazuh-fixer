---
# Test playbook for win_network_access_restrict_remote_sam role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-network-access-restrict-remote-sam-dev.yml

- name: Test Windows Network Access Restrict Remote SAM Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_network_access_restrict_remote_sam  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for RestrictRemoteSAM configuration."
