---
# Test playbook for win_prevent_printer_drivers role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-prevent-printer-drivers-dev.yml

- name: Test Windows Prevent Printer Drivers Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_prevent_printer_drivers  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for AddPrinterDrivers configuration."
