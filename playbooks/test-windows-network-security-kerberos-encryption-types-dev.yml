---
# Test playbook for win_network_security_kerberos_encryption_types role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-network-security-kerberos-encryption-types-dev.yml

- name: Test Windows Network Security Kerberos Encryption Types Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_network_security_kerberos_encryption_types  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for SupportedEncryptionTypes configuration."
