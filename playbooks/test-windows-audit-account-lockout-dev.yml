---
# Test playbook for win_audit_account_lockout role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-audit-account-lockout-dev.yml

- name: Test Windows Audit Account Lockout Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_account_lockout  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for Account Lockout audit policy configuration."
