---
# Test playbook for win_audit_pnp_activity role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-audit-pnp-activity-dev.yml

- name: Test Windows Audit PNP Activity Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_pnp_activity  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for Plug and Play Events audit policy configuration."
