---
# Test playbook for win_domain_require_strong_key role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-domain-require-strong-key-dev.yml

- name: Test Windows Domain Require Strong Key Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_domain_require_strong_key  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for RequireStrongKey configuration."
