---
# Test playbook for win_downloaded_maps_manager_disabled role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-downloaded-maps-manager-disabled-dev.yml

- name: Test Windows Downloaded Maps Manager Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_downloaded_maps_manager_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for MapsBroker Start configuration."
