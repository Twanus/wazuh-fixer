---
# Test playbook for win_interactive_logon_smart_card_removal role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-interactive-logon-smart-card-removal-dev.yml

- name: Test Windows Interactive Logon Smart Card Removal Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_interactive_logon_smart_card_removal  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for ScRemoveOption configuration."
