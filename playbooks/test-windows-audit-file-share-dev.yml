---
# Test playbook for win_audit_file_share role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-audit-file-share-dev.yml

- name: Test Windows Audit File Share Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_file_share  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for File Share audit policy configuration."
