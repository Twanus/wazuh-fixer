---
# Test playbook for win_network_security_minimum_session_security_ntlm_ssp_clients role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-network-security-minimum-session-security-ntlm-ssp-clients-dev.yml

- name: Test Windows Network Security Minimum Session Security NTLM SSP Clients Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_network_security_minimum_session_security_ntlm_ssp_clients  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for NTLMMinClientSec configuration."
