---
# Test playbook for win_audit_shutdown_on_fail role (Development)
# Usage: ansible-playbook -i ../inventory.ini playbooks/test-windows-audit-shutdown-on-fail-dev.yml

- name: Test Windows Audit Shutdown On Fail Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_audit_shutdown_on_fail  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for CrashOnAuditFail configuration."
