---
# Test playbook for win_uac_consent_prompt_behavior_admin role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-uac-consent-prompt-behavior-admin-dev.yml

- name: Test Windows UAC Consent Prompt Behavior Admin Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_uac_consent_prompt_behavior_admin  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for ConsentPromptBehaviorAdmin configuration."
