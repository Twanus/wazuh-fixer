---
# Test playbook for win_smb_v1_client_driver_disabled role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-smb-v1-client-driver-disabled-dev.yml

- name: Test Windows SMB v1 Client Driver Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_smb_v1_client_driver_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
