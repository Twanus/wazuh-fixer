---
# Test playbook for win_peer_networking_grouping_disabled role (Development)
# Usage: ansible-playbook -i inventory.ini playbooks/test-windows-peer-networking-grouping-disabled-dev.yml

- name: Test Windows Peer Networking Grouping Disabled Role (Development)
  hosts: windows
  gather_facts: true
  become: false
  roles:
    - role: ../roles/win_peer_networking_grouping_disabled  # noqa role-name[path]
  post_tasks:
    - name: Verify role completed successfully
      ansible.builtin.debug:
        msg: "Role execution completed. Check the output above for configuration."
