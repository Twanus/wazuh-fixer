---
# defaults file for win_audit_security_group_management
# CIS Benchmark 17.2.2 (check 26141) requires 'Audit Security Group Management' to include 'Success'
# This role uses auditpol.exe to configure the audit policy subcategory
# The required setting must include "Success" (can be "Success" or "Success and Failure")
