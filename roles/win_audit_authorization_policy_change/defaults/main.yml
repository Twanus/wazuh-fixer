---
# defaults file for win_audit_authorization_policy_change
# CIS Benchmark 17.7.3 (check 26157) requires 'Audit Authorization Policy Change' to include 'Success'
# This role uses auditpol.exe to configure the audit policy subcategory
# The required setting must include "Success" (can be "Success" or "Success and Failure")
