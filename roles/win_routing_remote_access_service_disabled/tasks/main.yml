---
# tasks file for win_routing_remote_access_service_disabled
- name: Check current RemoteAccess Start setting
  ansible.windows.win_shell: |
    $regPath = "HKLM:\SYSTEM\CurrentControlSet\Services\RemoteAccess"
    $regName = "Start"
    $value = Get-ItemProperty -Path $regPath -Name $regName -ErrorAction SilentlyContinue | Select-Object -ExpandProperty $regName
    if ($null -eq $value) {
      Write-Output "2"
    } else {
      Write-Output $value
    }
  register: win_routing_remote_access_service_disabled_current_setting
  changed_when: false

- name: Display current RemoteAccess Start setting
  ansible.builtin.debug:
    msg: >
      Current RemoteAccess Start: {{ win_routing_remote_access_service_disabled_current_setting.stdout | trim }}
      (0=Boot, 1=System, 2=Auto, 3=Manual, 4=Disabled [recommended])

- name: Set RemoteAccess Start to Disabled
  ansible.windows.win_regedit:
    path: HKLM:\SYSTEM\CurrentControlSet\Services\RemoteAccess
    name: Start
    data: "{{ win_routing_remote_access_service_disabled_start_value }}"
    type: dword
  register: win_routing_remote_access_service_disabled_apply_setting
  when: >
    (win_routing_remote_access_service_disabled_current_setting.stdout | trim | int) != (win_routing_remote_access_service_disabled_start_value | int)
  changed_when: true

- name: Report if no changes were needed
  ansible.builtin.debug:
    msg: >
      RemoteAccess Start is already set to {{ win_routing_remote_access_service_disabled_current_setting.stdout | trim }},
      which meets the requirement (4 = Disabled - Routing and Remote Access service is disabled)
  when: >
    (win_routing_remote_access_service_disabled_current_setting.stdout | trim | int) == (win_routing_remote_access_service_disabled_start_value | int)
