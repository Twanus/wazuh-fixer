---
# defaults file for win_limit_blank_password_use
# CIS Benchmark 2.3.1.3 (check 26010) requires 'Accounts: Limit local account use of blank passwords to console logon only' to be set to 'Enabled'
# This value can be overridden in playbooks or inventory
# Value 1 = Enabled (CIS requirement)
win_limit_blank_password_use_enabled_value: 1
