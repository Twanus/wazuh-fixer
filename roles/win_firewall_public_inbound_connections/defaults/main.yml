---
# defaults file for win_firewall_public_inbound_connections
# CIS Benchmark 9.3.2 (check 26131) requires 'Windows Firewall: Public: Inbound connections' to be set to 'Block (default)'
# This value can be overridden in playbooks or inventory
# Value 0 = Allow, 1 = Block (default) (CIS requirement)
win_firewall_public_inbound_connections_default_inbound_action: 1
